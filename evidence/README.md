# Evidence

`cultos-http-header-audit.json` was produced by `x402-seal` 0.1.0 before payer-signed evidence was required. It is retained as historical settlement data and is intentionally rejected by 0.1.1 `witness`.
