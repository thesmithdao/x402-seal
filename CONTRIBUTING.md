# Contributing

Open an issue before changing the supported payment matrix.

Pull requests must include deterministic coverage and must not require wallet secrets or funded network calls.

Run:

```bash
npm test
npm run typecheck
npm run build
```
